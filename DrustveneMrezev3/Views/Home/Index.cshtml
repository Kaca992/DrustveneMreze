@model PagedList.IPagedList<DrustveneMrezev3.Models.MovieListModel>
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron text-center">
    <h2>Don't know what to watch? Don't worry, we will find a movie You are going to enjoy, You just need to grab some popcorn and enjoy!</h2>
</div>
<div class="container">
    <!--<a href=@Url.Action("ShowUserMovies", "Manage")>
        <img class="frontPageImage" src="~/Resources/popcorn.png" />
    </a>-->
    @if (@Model.Count() != 0)
    {
        <h3>Popular movies:</h3>

        @Html.Partial("ShowMovies", @Model)

    }
</div>
